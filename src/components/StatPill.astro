---
interface Props {
  label: string;
  value: number | string;
}
const { label, value } = Astro.props;
---

<div class="text-center">
  <p class="text-2xl font-black">{value}</p>
  <p class="text-muted text-xs mt-0.5">{label}</p>
</div>
